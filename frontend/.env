# URL da API backend para desenvolvimento e produção
REACT_APP_API_URL=http://localhost:4000/api
# Workaround for Create React App host check when proxy is enabled and no LAN host is detected
DANGEROUSLY_DISABLE_HOST_CHECK=true
